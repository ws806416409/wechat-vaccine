server:
  port: 8089
spring:
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 100MB
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/vaccine?characterEncoding=utf-8&useSSL=false
    username: root
    password: root
  jdbc:
      template:
        query-timeout: 3
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl